myfile = open("words.txt")

def len(myfile):
	for letter in myfile:
		word=letter.strip()
		#word=letter
		while let in word:
			global let
			let =0
			let=word.readline()
			index = 0
			if let[index]<=len(word[:4]):
				print(i)
				index = index + 1
			return 0
len(myfile)
